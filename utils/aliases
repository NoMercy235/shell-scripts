# My aliases.
alias rm='rm -i'
alias cp='cp -i'
alias mv='mv -i'

# Docker aliases
alias dimg='docker image'
alias dcon='docker container'
alias dvol='docker volume'
alias dlogs='docker logs'
alias dinspect='docker inspect'
alias dinfo='docker info'

alias drun='docker container run'
alias dstart='docker container start'
alias dstop='docker container stop'
alias dattach='docker container attach'
alias dexec='docker container exec'
alias drm='docker container rm'
alias dps='docker container ls'

alias drmi='docker image rm'
alias dpull='docker image pull'
# Docker aliases

alias tome='chown -R nomercy235 ./*'

